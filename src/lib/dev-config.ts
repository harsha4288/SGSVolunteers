/**
 * Development configuration settings
 * These settings are only used in development mode
 */

export const DEV_CONFIG = {
  // Default date override for testing (format: YYYY-MM-DD)
  // Set to null to use the actual current date
  defaultDateOverride: "2025-07-10 00:00:00",
  
  // Whether to automatically apply the date override on page load
  autoApplyDateOverride: true,
};
